# GEMINI_API_KEY: Required for Gemini AI API calls.
# AI Studio automatically injects this at runtime from user secrets.
# Users configure this via the Secrets panel in the AI Studio UI.
GEMINI_API_KEY="MY_GEMINI_API_KEY"

# APP_URL: The URL where this applet is hosted.
# AI Studio automatically injects this at runtime with the Cloud Run service URL.
# Used for self-referential links, OAuth callbacks, and API endpoints.
APP_URL="MY_APP_URL"

# Wonca API Configuration
WONCA_URL="https://api-labs.wonca.com.br/wonca.labs.v1.LabsService/Track"
WONCA_AUTH="Apikey MY_WONCA_API_KEY"
TIMEOUT_MS=15000
MAX_CODES=200
CONCURRENCY=1

# Simulation Mode: Set to "true" to return mock data instead of calling Wonca
SIMULATION_MODE=false

# TRACKING_PROVIDER: "wonca" (default, paid), "scraper" (own scraper of the
# public Correios tracking site — see README for the ToS/reliability caveats)
# or "crnn" (same scraper, but captcha solving is delegated over HTTP to a
# separate correios-rastreamento service running its own trained CRNN model).
TRACKING_PROVIDER=wonca

# Only used when TRACKING_PROVIDER=scraper.
# CAPTCHA_SOLVER: "ocr" (free, best-effort, default) or "2captcha"/"anticaptcha"
# (paid third-party solving service, more reliable).
CAPTCHA_SOLVER=ocr
# Required only when CAPTCHA_SOLVER is "2captcha" or "anticaptcha".
CAPTCHA_API_KEY=""
# How many times to retry with a fresh captcha before giving up on one check.
SCRAPER_MAX_CAPTCHA_RETRIES=3
# Minimum delay (ms) between requests to the Correios tracking site.
SCRAPER_REQUEST_DELAY_MS=1500

# Only used when TRACKING_PROVIDER=crnn. Base URL of a running
# correios-rastreamento instance (https://github.com/opastorello/correios-rastreamento).
RASTREAMENTO_SERVICE_URL=http://localhost:8003
# Bearer token, only if that instance has API_TOKEN configured.
RASTREAMENTO_SERVICE_TOKEN=""

# Postgres connection string (Neon or Vercel Postgres) used by the
# tracking-subscriptions/webhook feature and its migration script.
DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"

# Shared secret for /api/cron/run. When set as a Vercel env var, Vercel
# automatically sends it as "Authorization: Bearer $CRON_SECRET" on
# scheduled Cron Job invocations.
CRON_SECRET="MY_CRON_SECRET"
